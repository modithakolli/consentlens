const json = async (url, options = {}) => { const response = await fetch(url, options); const body = await response.json(); if (!response.ok) throw new Error(body.error || "Request failed"); return body; };
const show = (id, value) => { const node = document.getElementById(id); if (node) node.textContent = typeof value === "string" ? value : JSON.stringify(value, null, 2); };
document.getElementById("profileForm")?.addEventListener("submit", async (event) => { event.preventDefault(); try { const domain = new FormData(event.currentTarget).get("domain"); show("result", (await json(`/public-profiles/${encodeURIComponent(domain)}`)).profile); } catch (error) { show("result", error.message); } });
document.getElementById("contributionForm")?.addEventListener("submit", async (event) => { event.preventDefault(); try { const form = new FormData(event.currentTarget); show("contributionResult", await json("/intel-contributions", { method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify({ domain:form.get("domain"), kind:form.get("kind"), summary:form.get("summary"), sourceUrls:String(form.get("sourceUrls")).split(/\n+/) }) })); } catch (error) { show("contributionResult", error.message); } });
const token = () => document.getElementById("reviewerToken")?.value || "";
document.getElementById("loadClaims")?.addEventListener("click", async () => { try { show("claims", (await json("/reviewer/claims", { headers:{"X-ConsentLens-Reviewer-Token":token()} })).claims); } catch (error) { show("claims", error.message); } });
document.getElementById("loadContributions")?.addEventListener("click", async () => { try { show("contributions", (await json("/reviewer/contributions", { headers:{"X-ConsentLens-Reviewer-Token":token()} })).contributions); } catch (error) { show("contributions", error.message); } });
document.getElementById("loadObservations")?.addEventListener("click", async () => { try { show("observations", (await json("/reviewer/observations", { headers:{"X-ConsentLens-Reviewer-Token":token()} })).observations); } catch (error) { show("observations", error.message); } });
document.getElementById("observationReviewForm")?.addEventListener("submit", async (event) => { event.preventDefault(); try { const value = Object.fromEntries(new FormData(event.currentTarget)); show("observationReviewResult", await json(`/reviewer/observations/${encodeURIComponent(value.host)}`, { method:"POST", headers:{"Content-Type":"application/json","X-ConsentLens-Reviewer-Token":token()}, body:JSON.stringify(value) })); } catch (error) { show("observationReviewResult", error.message); } });
document.getElementById("reviewForm")?.addEventListener("submit", async (event) => { event.preventDefault(); try { const value = Object.fromEntries(new FormData(event.currentTarget)); show("reviewResult", await json("/verification-reviews", { method:"POST", headers:{"Content-Type":"application/json","X-ConsentLens-Reviewer-Token":token()}, body:JSON.stringify(value) })); } catch (error) { show("reviewResult", error.message); } });
